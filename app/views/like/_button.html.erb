<%
  like = likable.likes.where(user_id: current_user.id)
  button_text = like && like.count > 0 ? "Unlike" : "Like"
%>
<div class="A_Like">
  <%= link_to button_text, like_toggle_path(type: likable.class, id: likable.id), data: {turbo_stream: true} %>
</div>