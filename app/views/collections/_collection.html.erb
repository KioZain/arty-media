<div  class="O_Collection"  id="<%= dom_id collection %>">
  <div class="M_CollectionDescription">
    <h1><%= collection.title %></h1>
    <h3><%= collection.body %></h3>
  </div>
  <p><%= link_to "Посмотреть коллекцию", collection_path(collection) %></p>

</div>
